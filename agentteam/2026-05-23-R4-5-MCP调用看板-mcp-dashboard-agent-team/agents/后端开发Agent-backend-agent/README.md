# 后端开发Agent Backend Agent

后端开发 Agent 负责检查 MCP Preview API、调用日志和测试是否足够支撑 R4-5。

## 文件说明

- `PROMPT.md`：新会话启动后端 Agent 时使用。
- `SKILL.md`：后端 Agent 的执行规则。
- `WORKSPACE.md`：后端 Agent 的文件边界。
- `STATUS.md`：后端 Agent 的当前执行状态。
