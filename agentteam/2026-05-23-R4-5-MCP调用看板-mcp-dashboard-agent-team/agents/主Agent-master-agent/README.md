# 主Agent Master Agent

主 Agent 是 R4-5 的统筹者，负责把任务拆解给专业 Agent，并在专业 Agent 完成后进行检查、集成、验证和发布。

## 文件说明

- `PROMPT.md`：新会话启动主 Agent 时使用的提示词。
- `SKILL.md`：主 Agent 的项目内技能规则。
- `WORKSPACE.md`：主 Agent 允许修改和禁止修改的文件范围。
- `STATUS.md`：主 Agent 当前状态。
- `CONTEXT.md`：主 Agent 的当前上下文、偏好、决策和续接说明，用于防止会话丢失。
